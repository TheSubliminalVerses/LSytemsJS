declare module 'index' {
    global {
        const _map: RuleMap
        const axiom: string

        let sentence: string
        let angle: number
        let len: number
        let inp: any
    }
}